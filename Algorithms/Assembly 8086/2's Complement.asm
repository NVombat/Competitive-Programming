DATA SEGMENT
    N1 DB 00010000B
    RES DB ?
DATA ENDS

CODE SEGMENT
    ASSUME CS:CODE, DS:DATA

    START:
    MOV AX, DATA
    MOV DS, AX

    MOV AH, 0000H
    MOV AL, N1

    NOT AL

    ADD AL, 00000001B
    MOV RES, AL

    CODE ENDS

END START
